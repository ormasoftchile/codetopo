Key decision: keep refs-backed same-file caller recovery as the source of truth for HOF-wrapped calls, and allow callers_approx exact_then_candidates to fall back to refs whenever the exact edge results contain only file-node callers. Verified from extractor.cpp that anonymous JS/TS function expressions do not push a new symbol scope, so a call like runLayout() inside time(..., function(){ ... }) still records the enclosing named function (layout) as containing_symbol_index/containing_node_id. The bug is candidate gating in tools.cpp: a file-node exact edge from bundled output suppressed refs-backed candidates, hiding the real same-file source caller.
