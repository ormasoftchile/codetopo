# Orchestration Log: Grag (2026-03-31T2237Z)

**Agent:** Grag (Systems Engineer)  
**Task:** Implement DEC-028 R1-R4 extraction timeout recommendations  
**Outcome:** ✅ SUCCESS

## Deliverables

### R1: Extraction timeout (HIGHEST PRIORITY)
- Added `Config::extraction_timeout_s = 10` — separate from `parse_timeout_s`
- New `--extract-timeout` CLI flag on `index` and `init` subcommands
- Supervisor passes the value to child process via `--extract-timeout` arg
- Extractor constructor now receives extraction-specific timeout

### R2: Lower default max-file-size
- Changed `max_file_size_kb` default from 10240 (10MB) to 512 (512KB)
- Updated config.h, main.cpp (both index and init subcommand defaults)

### R3: Remove redundant tree-sitter set_timeout
- Removed `parser.set_timeout()` call from cmd_index.cpp worker lambda
- Rationale: Parse phase averages 1.8ms/file; extraction timeout is the real defense

### R4: Log slow files
- Added `extract_us` field to ParsedFile for per-file extraction timing
- Files exceeding 2 seconds log `[SLOW]` tag with extract time and symbol count
- Summary line includes slow file count

### Unblocking Joan's tests
- Enabled blocked test case in test_extraction_timeout.cpp (Scenario 4)

## Verification

- **Build:** Release build clean, 0 errors, 0 warnings
- **Tests:** All 185 tests pass (964 assertions), up from 179 (939)
- **Impact:** Joan's 6-test DEC-028 suite fully enabled

## Files Modified

- `src/config.h` — Config::extraction_timeout_s field
- `src/main.cpp` — CLI flags, defaults
- `src/cli/cmd_index.cpp` — Remove parse_timeout call, pass extraction_timeout to Extractor
- `src/cli/cmd_init.h` — Pass --extract-timeout to supervisor
- `src/index/supervisor.cpp` — Plumb --extract-timeout arg
- `tests/unit/test_extraction_timeout.cpp` — Unblock test 4

## Team Awareness

Joan's proactive test suite (DEC-028 extraction timeout tests) is now fully validated and passing.
