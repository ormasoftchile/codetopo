# Orchestration — Otho (2026-04-01T16:00Z)

**Agent:** Otho (Performance Engineer)  
**Mode:** background  
**Task:** Profile parse+extract bottleneck after DEC-032 persist optimization

## Outcome

✅ **Profiled 4 subsets (tiny/small/fsm/fsm@1MB).** Results:
- Persist **still #1 bottleneck** — workers 788 files/s, persist caps 617 files/s
- Contention 35% of wall from bursty arrival (largest-first sort causes wave effects)
- ThreadPool stacks still 64MB (DEC-021 never applied) = 1.15GB committed memory

## Findings

1. **Persist remains constraining phase** despite DEC-032 improvements (3.3→1.6ms/file)
2. **Contention is bursty** — largest-first sort → workers finish in waves → persist burst
3. **file_read explodes at 1024KB** — 9.4x slowdown, validates 512KB max-file-size default
4. **Parse dominates at 512KB** (46-70% of worker time) — tree-sitter cost, limited optimization potential
5. **Extract well-controlled** (3-6ms/file) — DEC-029 timeout working correctly
6. **ThreadPool 64MB stacks** — DEC-021 fix never applied
7. **WAL checkpoint 18% of wall at 1024KB**
8. **Per-file persist scales** with symbol count

## Ranked Recommendations

| # | Priority | Title | Effort | Impact |
|---|----------|-------|--------|--------|
| R1 | HIGH | Fix ThreadPool stack 64→8MB | 1-line | 1GB+ RAM savings, zero risk |
| R2 | HIGH | Pipelined persist thread | ~200 LOC | Eliminate 35% contention, 788→~450 files/s |
| R3 | MEDIUM | Parallel WAL persist (DEC-026 R7) | Higher complexity | Higher ceiling if R2 insufficient |
| R4 | LOW | Memory-mapped file reading | Moderate | Small wall impact at 512KB |
| R5 | LOW | Defer WAL checkpoint (turbo mode) | Small | 1.3–6.1s savings |

## Artifacts Modified

- `src/cli/cmd_index.cpp` — profiler instrumentation, config fields, `--profile` flag
- `src/util/arena.h` + `arena.cpp` — fixed duplicate definitions
- `src/core/tree_guard.h` — move assignment operator
- Tests: all 173 pass (914 assertions)

## Decision Outcome

**New decision created:** DEC-034 (now in decisions.md)

---

*Orchestrated by Scribe at 2026-04-01T16:00Z after Otho profiling session.*
