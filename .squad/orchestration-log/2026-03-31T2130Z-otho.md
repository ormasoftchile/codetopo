# Orchestration Log: Init Command Profiling Harness

**Date:** 2026-03-31  
**Trigger:** User requested performance profiling infrastructure for init command optimization

## Agent Spawned

| Agent | Role | Assignment | Status |
|-------|------|------------|--------|
| Otho | Performance Engineer | Implemented --profile flag with 17-phase comprehensive profiling report for init & index commands | ✅ SUCCESS |

## Work Delivered

**Scope:** Add production-grade profiling harness to measure all phases in the init→index pipeline.

**Implementation:**
1. **New file:** `src/util/profiler.h` — `Profiler` struct, `PhaseTimer`, `ScopedPhase` RAII types
2. **Config:** Added `bool profile` field to `Config` struct; `--profile` CLI flag on `index` and `init` subcommands
3. **Supervisor:** Routes `--profile` flag to child process
4. **cmd_index.cpp:** Wrapped all 17 phases with `ScopedPhase` timers; persist timing added to always-on summary line

**Phases Instrumented:**
- Main thread: scan, change detection, quarantine rehab, index rebuild (read-path), resolve references, index rebuild (write-path), FTS rebuild, WAL checkpoint
- Worker threads: arena routing, file read, hash computation, parse, extraction
- Parallel overhead tracking

## Verification

- **Build:** Release build, 0 errors, 0 warnings
- **Tests:** 179 tests pass (939 assertions)
- **Benchmark:** Tested on codetopo repo (82 files) and user's production target (162K+ files)
- **Key Finding:** Persist dominates at ~49% of total wall time; parallel efficiency only ~7% on small repos due to per-thread arena overhead

## Decisions Recorded

- DEC-029: Profile flag and 17-phase profiling report instrumentation (accepted)

## Next Steps

- Otho's findings (persist 49% of time, parallel efficiency 7%) inform prioritization for next performance cycle
- Profile data available for team to guide optimization efforts
- Single-run profiling captures variance; future work could add averaging across multiple iterations
